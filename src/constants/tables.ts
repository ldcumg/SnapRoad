enum tables {
  profiles = 'profiles',
  group = 'group',
  user_group = 'user_group',
  posts = 'posts',
  images = 'images',
  tags = 'tags',
  comment = 'comment',
  drafts = 'drafts',
}

export default tables;
