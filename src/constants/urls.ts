export const LOGIN_PAGE = '/login'; // TODO 삭제 후 아래 것으로 통일
export const HOME = '/';

enum URLS {
  home = '/',
  signUp = '/signup',
  logIn = '/login',
  admin = '/admin',
  groupDetail = '/group',
  groupList = '/grouplist',
  makeGroup = '/makegroup',
  myPage = '/mypage',
}

export default URLS;
